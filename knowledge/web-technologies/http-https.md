# HTTPとHTTPS

## 違いは「暗号化されているかどうか」だけ

HTTPとHTTPSの違いは、通信が暗号化されているかどうかの一点。HTTPSの「S」は Secure（安全）の意味で、HTTP通信をTLSで暗号化したものがHTTPS。

注意したいのは、HTTPSにしても「状態を持つ（ステートフルになる）」わけではないこと。HTTPもHTTPSも、どちらも本来ステートレス（リクエストごとに「この人は誰？」を覚えていない）なのは変わらない。
