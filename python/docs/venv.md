### 仮想環境とは

1. パッケージの保存先を専用フォルダに切り替える
2. どのPythonを使うかのパスを切り替える

仮想環境は「別のPythonをインストールする」のではなく、PC本体にすでにインストールされているPythonを参照し、パッケージの保存先だけを分離した環境を作る仕組み。

PCにPython 3.9と3.13が両方入っている場合、`python3.13 -m venv venv`とすれば3.13を参照する仮想環境が作られる。3.13自体はPC本体にすでにインストールされている必要がある。
